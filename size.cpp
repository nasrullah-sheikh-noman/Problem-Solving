#include <bits/stdc++.h>
using namespace std;

int main () {

  string s = "noman";
  auto si = s.end();
  cout << *si << endl;
  for (int i = 0; i < s.size(); i++) {
    cout << s[i] << endl;
  }

  return 0;
}